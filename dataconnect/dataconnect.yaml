specVersion: "v1"
serviceId: "student-dashboard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "student-dashboard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
